/**
 * Nova Agent — Tool Types
 *
 * Re-exported for clarity. Each tool is defined as a NovaTool
 * which is then converted to AI SDK tool format by the registry.
 */

export type { NovaTool } from '../types.js';
